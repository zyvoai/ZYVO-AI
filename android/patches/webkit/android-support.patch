diff --git a/Source/JavaScriptCore/runtime/InitializeThreading.cpp b/Source/JavaScriptCore/runtime/InitializeThreading.cpp
index 250e62b5ef..88f294b668 100644
--- a/Source/JavaScriptCore/runtime/InitializeThreading.cpp
+++ b/Source/JavaScriptCore/runtime/InitializeThreading.cpp
@@ -137,6 +137,19 @@ void initialize()
         if (VM::isInMiniMode())
             WTF::fastEnableMiniMode(Options::forceMiniVMMode());
 
+#if defined(__ANDROID__)
+        // Android app processes (e.g., Termux) run under a strict seccomp filter
+        // that blocks certain syscalls, and debuggerd intercepts SIGSEGV before
+        // the app's signal handler. Force polling traps and disable Wasm signal
+        // handler to avoid crashes.
+        {
+            Options::AllowUnfinalizedAccessScope scope;
+            Options::usePollingTraps() = true;
+            Options::useWasmFaultSignalHandler() = false;
+            Options::useWasmFastMemory() = false;
+        }
+#endif
+
         if (Wasm::isSupported() || !Options::usePollingTraps()) {
             if (!Options::usePollingTraps())
                 VMTraps::initializeSignals();
diff --git a/Source/bmalloc/bmalloc/DebugHeap.cpp b/Source/bmalloc/bmalloc/DebugHeap.cpp
index b60ad015e8..58060dac58 100644
--- a/Source/bmalloc/bmalloc/DebugHeap.cpp
+++ b/Source/bmalloc/bmalloc/DebugHeap.cpp
@@ -122,7 +122,13 @@ void* DebugHeap::malloc(size_t size, FailureAction action)
 
 void* DebugHeap::memalign(size_t alignment, size_t size, FailureAction action)
 {
+#if defined(__ANDROID__)
+    void* result = nullptr;
+    if (::posix_memalign(&result, alignment, size) != 0)
+        result = nullptr;
+#else
     void* result = ::aligned_alloc(alignment, size);
+#endif
     RELEASE_BASSERT(action == FailureAction::ReturnNull || result);
     return result;
 }
diff --git a/Source/bmalloc/libpas/src/libpas/pas_min_heap.h b/Source/bmalloc/libpas/src/libpas/pas_min_heap.h
index 72ab2c198e..240d0f69dc 100644
--- a/Source/bmalloc/libpas/src/libpas/pas_min_heap.h
+++ b/Source/bmalloc/libpas/src/libpas/pas_min_heap.h
@@ -29,6 +29,7 @@
 #include "pas_allocation_config.h"
 #include "pas_utils.h"
 #include <stdio.h>
+#include <string.h> /* for memcmp (bcmp replacement on Android) */
 
 PAS_BEGIN_EXTERN_C;
 
@@ -260,7 +261,7 @@ PAS_BEGIN_EXTERN_C;
         \
         size_t index = config.get_index(&element); \
         PAS_ASSERT(index); \
-        PAS_ASSERT(!bcmp(name##_get_ptr_by_index(min_heap, index), &element, sizeof(type))); \
+        PAS_ASSERT(!memcmp(name##_get_ptr_by_index(min_heap, index), &element, sizeof(type))); \
         name##_remove_by_index(min_heap, index); \
     } \
     \
diff --git a/Source/bmalloc/libpas/src/libpas/pas_probabilistic_guard_malloc_allocator.c b/Source/bmalloc/libpas/src/libpas/pas_probabilistic_guard_malloc_allocator.c
index cb1c7290db..a145947312 100644
--- a/Source/bmalloc/libpas/src/libpas/pas_probabilistic_guard_malloc_allocator.c
+++ b/Source/bmalloc/libpas/src/libpas/pas_probabilistic_guard_malloc_allocator.c
@@ -45,8 +45,8 @@
 #include <sys/mman.h>
 #include <unistd.h>
 
-/* PlayStation does not currently support the backtrace API. */
-#if !PAS_PLATFORM(PLAYSTATION) && __has_include (<execinfo.h>)
+/* PlayStation and Android (API < 33) do not currently support the backtrace API. */
+#if !PAS_PLATFORM(PLAYSTATION) && !defined(__ANDROID__) && __has_include (<execinfo.h>)
 #include <execinfo.h>
 #else
 size_t backtrace(void** buffer, size_t size)
diff --git a/Source/bmalloc/libpas/src/libpas/pas_thread_local_cache.c b/Source/bmalloc/libpas/src/libpas/pas_thread_local_cache.c
index 494c1bb50e..946b885652 100644
--- a/Source/bmalloc/libpas/src/libpas/pas_thread_local_cache.c
+++ b/Source/bmalloc/libpas/src/libpas/pas_thread_local_cache.c
@@ -214,6 +214,9 @@ static void dump_thread_diagnostics(pthread_t thread)
 #endif
 #if PAS_PLATFORM(PLAYSTATION)
     getname_result = pthread_get_name_np(thread, thread_name);
+#elif defined(__ANDROID__)
+    /* pthread_getname_np requires API 26+, stub for API 24 */
+    getname_result = -1;
 #else
     getname_result = pthread_getname_np(thread, thread_name, sizeof(thread_name));
 #endif
