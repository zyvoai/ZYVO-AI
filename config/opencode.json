{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "PLACEHOLDER — run `node config/generate.mjs <baseURL> <apiKey>` with the real omniroute endpoint + key to generate the ranked top-50 model list. Replace the values below meanwhile.",
  "model": "zyvo/PLACEHOLDER-MODEL",
  "provider": {
    "zyvo": {
      "name": "Zyvo",
      "npm": "@ai-sdk/openai-compatible",
      "options": {
        "baseURL": "https://YOUR-OMNIROUTE-ENDPOINT/v1",
        "apiKey": "YOUR-OMNIROUTE-KEY"
      },
      "models": {
        "PLACEHOLDER-MODEL": {
          "name": "Zyvo default model"
        }
      }
    }
  }
}
